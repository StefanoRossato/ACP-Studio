desc:Decoder Test
options:gmem=ACP_STUDIO

@init

REG_COMMAND = 0;

CMD_IDLE  = 0;
CMD_RESET = 1;

@sample

command = gmem[REG_COMMAND];

command == CMD_RESET ? (
    gmem[REG_COMMAND] = CMD_IDLE;
);

spl0 = spl0;
spl1 = spl1;